<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pang.blog.dao.UserMapper" >
  <resultMap id="BaseResultMap" type="com.pang.blog.entity.User" >
    <result column="username" property="username" jdbcType="VARCHAR" />
    <result column="hashword" property="hashword" jdbcType="VARCHAR" />
    <result column="salt" property="salt" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="com.pang.blog.entity.User" >
    insert into user (username, hashword, salt
      )
    values (#{username,jdbcType=VARCHAR}, #{hashword,jdbcType=VARCHAR}, #{salt,jdbcType=VARCHAR}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.pang.blog.entity.User" >
    insert into user
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="username != null" >
        username,
      </if>
      <if test="hashword != null" >
        hashword,
      </if>
      <if test="salt != null" >
        salt,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="username != null" >
        #{username,jdbcType=VARCHAR},
      </if>
      <if test="hashword != null" >
        #{hashword,jdbcType=VARCHAR},
      </if>
      <if test="salt != null" >
        #{salt,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="getSalt" resultType="java.lang.String">
    select
    salt
    from user
  </select>
  <select id="getHash" resultType="java.lang.String" >
    select
    hashword
    from user
  </select>
  <update id="updateUser" parameterType="Map">
    update user
    set username = #{username,jdbcType=VARCHAR},
      hashword = #{hashword,jdbcType=VARCHAR}
  </update>
  <select id="selectUser" resultMap="BaseResultMap" parameterType="java.lang.String">
    select
    username,hashword,salt
    from user
    where username= #{username,jdbcType=VARCHAR}
  </select>
</mapper>